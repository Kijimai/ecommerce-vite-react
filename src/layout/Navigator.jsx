import styled from "styled-components"

const Navigator = () => {
  return (
    <NavigatorWrapper>
      <nav>
        <h2>Fasdasdasd</h2>
      </nav>
    </NavigatorWrapper>
  )
}

const NavigatorWrapper = styled.header`
  padding: 2.4rem;
`

export default Navigator
